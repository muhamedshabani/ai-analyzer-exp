"use client";

import AppShell from "@/components/AppShell";
import { api } from "@/lib/api";
import { Analysis, Project } from "@/lib/types";
import { AutoAwesome, EmailOutlined } from "@mui/icons-material";
import { Alert, Box, Button, Chip, Divider, Grid2 as Grid, Paper, Skeleton, Stack, Typography } from "@mui/material";
import { useParams } from "next/navigation";
import { ReactNode, useCallback, useEffect, useState } from "react";

function AnalysisCard({ title, children, wide = false }: { title: string; children: ReactNode; wide?: boolean }) { return <Grid size={{ xs: 12, md: wide ? 12 : 6 }}><Paper elevation={0} sx={{ p: 3, border: "1px solid #e5e9f2", height: "100%" }}><Typography variant="overline" color="primary" fontWeight={800}>{title}</Typography><Typography component="div" sx={{ whiteSpace: "pre-line", mt: 1, lineHeight: 1.75 }}>{children}</Typography></Paper></Grid>; }

export default function RequestDetailsPage() {
  const { id } = useParams<{ id: string }>(); const [item, setItem] = useState<Project>(); const [error, setError] = useState(""); const [notice, setNotice] = useState(""); const [busy, setBusy] = useState<"analyze" | "send" | "">("");
  const load = useCallback(() => { setError(""); api<Project>(`/project-requests/${id}`).then(setItem).catch(x => setError(x.message)); }, [id]);
  useEffect(load, [load]);
  async function analyze() { setBusy("analyze"); setError(""); setNotice(""); try { const analysis = await api<Analysis>(`/project-requests/${id}/analyze`, { method: "POST" }); setItem(current => current ? { ...current, status: "Analyzed", analysis } : current); setNotice("Analysis generated successfully."); } catch (x) { setError(x instanceof Error ? x.message : "Analysis failed."); } finally { setBusy(""); } }
  async function send() { setBusy("send"); setError(""); setNotice(""); try { await api(`/project-requests/${id}/send-reply`, { method: "POST" }); setNotice("Email reply simulated successfully."); load(); } catch (x) { setError(x instanceof Error ? x.message : "Could not simulate email."); } finally { setBusy(""); } }
  return <AppShell role="Admin">{error && <Alert severity="error" sx={{ mb: 2 }}>{error}</Alert>}{notice && <Alert severity="success" sx={{ mb: 2 }}>{notice}</Alert>}{!item ? <Skeleton variant="rounded" height={360} /> : <><Stack direction={{ xs: "column", md: "row" }} justifyContent="space-between" alignItems={{ md: "center" }} gap={2} mb={3}><div><Stack direction="row" gap={1} alignItems="center"><Typography variant="h4">{item.projectTitle}</Typography><Chip label={item.status} color={item.analysis ? "success" : "default"} /></Stack><Typography color="text.secondary">Submitted by {item.clientName} · {item.clientEmail}</Typography></div><Stack direction="row" gap={1}><Button variant="contained" startIcon={<AutoAwesome />} onClick={analyze} disabled={!!busy}>{busy === "analyze" ? "Analyzing…" : item.analysis ? "Analyze again" : "Analyze with AI"}</Button><Button variant="outlined" startIcon={<EmailOutlined />} onClick={send} disabled={!item.analysis || !!busy}>{busy === "send" ? "Sending…" : "Send / simulate email"}</Button></Stack></Stack><Paper elevation={0} sx={{ p: 3, border: "1px solid #e5e9f2", mb: 3 }}><Grid container spacing={2}><Grid size={{ xs: 12, md: 8 }}><Typography variant="overline" color="text.secondary">Project brief</Typography><Typography sx={{ whiteSpace: "pre-line", lineHeight: 1.7 }}>{item.projectDescription}</Typography></Grid><Grid size={{ xs: 12, md: 4 }}><Stack spacing={1}><Typography><b>Industry:</b> {item.industry}</Typography><Typography><b>Budget:</b> {item.budgetRange}</Typography><Typography><b>Deadline:</b> {item.desiredDeadline ? new Date(item.desiredDeadline).toLocaleDateString() : "Flexible"}</Typography><Typography><b>Company:</b> {item.companyName || "—"}</Typography></Stack></Grid></Grid></Paper>{item.analysis ? <><Stack direction={{ xs: "column", sm: "row" }} justifyContent="space-between" mb={2}><Typography variant="h5">AI analysis</Typography><Stack direction="row" gap={1}><Chip label={`${item.analysis.complexityLevel} complexity`} color="primary" variant="outlined" /><Chip label={item.analysis.estimatedTimeline} /></Stack></Stack><Grid container spacing={2}><AnalysisCard title="Project summary" wide>{item.analysis.projectSummary}</AnalysisCard><AnalysisCard title="Functional requirements">{item.analysis.functionalRequirements}</AnalysisCard><AnalysisCard title="Suggested modules">{item.analysis.suggestedModules}</AnalysisCard><AnalysisCard title="Technology">{item.analysis.suggestedTechStack}</AnalysisCard><AnalysisCard title="Estimated cost">{item.analysis.estimatedCostRange}</AnalysisCard><AnalysisCard title="Recommended team" wide>{item.analysis.recommendedTeam}</AnalysisCard><AnalysisCard title="Clarification questions">{item.analysis.clarificationQuestions}</AnalysisCard><AnalysisCard title="Risks and assumptions">{item.analysis.risksAndAssumptions}</AnalysisCard><AnalysisCard title="Client reply draft" wide>{item.analysis.clientReplyDraft}</AnalysisCard></Grid></> : <Paper elevation={0} sx={{ p: 5, textAlign: "center", border: "1px dashed #b8c2d8" }}><AutoAwesome color="primary" sx={{ fontSize: 42 }} /><Typography variant="h6" mt={1}>Ready for analysis</Typography><Typography color="text.secondary">Generate requirements, estimates, risks, and a suggested delivery team.</Typography></Paper>}</>}</AppShell>;
}
